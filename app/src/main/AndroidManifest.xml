<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.w2w_texi">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.W2W_texi">
        <activity android:name=".SettingsActivity2"></activity>
        <activity android:name=".DriversMapActivity" />
        <activity android:name=".CustomersMapActivity" />
        <activity
            android:name=".DriverLoginActivity"
            android:label="@string/title_activity_driver_login"
            android:theme="@style/Theme.W2W_texi.NoActionBar" />
        <activity android:name=".CustomerLoginActivity2" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>